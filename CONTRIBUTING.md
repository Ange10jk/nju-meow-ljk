# 贡献指南

感谢你对南雍猫札的关注！我们欢迎任何形式的贡献。

## 提交 Issue

如果你发现了 Bug 或有功能建议，请通过 GitHub Issue 提交。

提交 Bug 报告时请包含：
- 问题描述
- 复现步骤
- 预期行为与实际行为
- 截图（如有）
- 设备与微信版本信息

## 提交 Pull Request

1. Fork 本仓库
2. 创建你的功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交你的修改 (`git commit -m 'feat: add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建一个 Pull Request

### Commit 规范

推荐使用约定式提交：

- `feat:` 新功能
- `fix:` Bug 修复
- `docs:` 文档更新
- `style:` 代码格式（不影响功能）
- `refactor:` 重构
- `chore:` 构建/工具变更

### 代码风格

- 使用 `const` / `let`，避免 `var`
- 保持与现有代码一致的缩进（2 空格）
- 提交前清理 `console.log` 调试日志

## 行为准则

请保持友善和尊重。我们致力于为每个人营造一个包容的社区环境。
